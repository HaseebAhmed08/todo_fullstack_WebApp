{
  "$schema": "http://json.schemastore.org/railway",
  "railway": {
    "buildCommand": "pip install -r requirements.txt",
    "startCommand": "uvicorn src.main:app --host 0.0.0.0 --port $PORT",
    "watchPatterns": [
      "src/**",
      "requirements.txt"
    ]
  }
}
